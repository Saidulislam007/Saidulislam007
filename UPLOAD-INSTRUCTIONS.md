# Upload instructions

1. Open your profile repository:
   `https://github.com/Saidulislam007/Saidulislam007`
2. Upload `README.md`.
3. Upload the complete `assets` folder without renaming it.
4. Commit the files to the repository's default branch.

The final repository structure must remain:

```text
Saidulislam007/
├── README.md
└── assets/
    └── saidul-profile.webp
```

The profile is written with real GitHub-supported Markdown and HTML. Only the
personal portrait is an image. The profile is intentionally static and
contains no animation.
